namespace ElenarRPG
{
    class Weaponary
    {
     /*   static Random numberGen = new Random();

        public int damage;
        // fists weapon
        public Fists(int damage)
        {
            // damage random 
            damage = numberGen.Next(0,1);
        } */
    }
}